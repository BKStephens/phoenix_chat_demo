<h2>New conversation</h2>

<%= render "form.html", changeset: @changeset,
                        action: conversation_path(@conn, :create) %>

<%= link "Back", to: conversation_path(@conn, :index) %>
