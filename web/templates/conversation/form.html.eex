<%= form_for @changeset, @action, fn f -> %>
  <%= if @changeset.action do %>
    <div class="alert alert-danger">
      <p>Oops, something went wrong! Please check the errors below:</p>
      <ul>
        <%= for {attr, message} <- f.errors do %>
          <li><%= humanize(attr) %> <%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= label f, :message, "Message", class: "control-label" %>
    <%= text_input f, :message, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :user_id, "User", class: "control-label" %>
    <%= number_input f, :user_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= label f, :parent_id, "Parent", class: "control-label" %>
    <%= number_input f, :parent_id, class: "form-control" %>
  </div>

  <div class="form-group">
    <%= submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
